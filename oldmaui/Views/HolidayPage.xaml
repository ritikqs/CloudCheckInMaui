<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="CloudCheckInMaui.Views.HolidayPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converter="clr-namespace:CloudCheckInMaui.Converter"
    xmlns:header="clr-namespace:CloudCheckInMaui.Views.Headers"
    xmlns:icons="clr-namespace:CloudCheckInMaui.Fonts"
    xmlns:translater="clr-namespace:CloudCheckInMaui.Resources"
    Title="Holiday"
    BackgroundColor="White"
    Shell.NavBarIsVisible="False">
    <ContentPage.Resources>
        <ResourceDictionary>
            <converter:LeaveStatusConverter x:Key="Leavestatusconverter" />
            <converter:LeaveStatusColorConverter x:Key="Leavestatuscolorconverter" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <Grid RowDefinitions="50,40,*">
            <header:CustomTitleBar Grid.Row="0" />
            <Label
                Grid.Row="1"
                FontFamily="MediumFont"
                FontSize="18"
                HorizontalOptions="Center"
                Text="Holiday Calendar"
                TextColor="Black"
                VerticalOptions="Center" />
            <RefreshView
                Grid.Row="2"
                IsRefreshing="{Binding IsListRefreshing}"
                Command="{Binding RefreshHolidayListCommand}">
                <CollectionView
                    x:Name="holidayList"
                    Margin="10"
                    ItemsSource="{Binding HolidayRequests}"
                    VerticalOptions="FillAndExpand">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame
                                Margin="5"
                                Padding="10,5"
                                BackgroundColor="White"
                                BorderColor="LightGray"
                                CornerRadius="8"
                                HeightRequest="60">
                                <Grid
                                    BackgroundColor="White"
                                    ColumnDefinitions="15,*,100"
                                    ColumnSpacing="5"
                                    VerticalOptions="Fill">
                                    <BoxView
                                        Grid.Column="0"
                                        Margin="0,15,0,0"
                                        Color="{StaticResource NewThemeTextColor}"
                                        CornerRadius="5"
                                        HeightRequest="10"
                                        HorizontalOptions="End"
                                        VerticalOptions="Start"
                                        WidthRequest="10" />
                                    <Label
                                        Grid.Column="1"
                                        Margin="0,10,0,0"
                                        HorizontalOptions="Fill"
                                        HorizontalTextAlignment="Start"
                                        MaxLines="2"
                                        VerticalOptions="Start"
                                        VerticalTextAlignment="Start">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span
                                                    FontFamily="MediumFont"
                                                    Text="Leave Applied"
                                                    TextColor="Gray" />
                                                <Span
                                                    FontFamily="LightFont"
                                                    Text=" from "
                                                    TextColor="Gray" />
                                                <Span
                                                    FontFamily="LightFont"
                                                    Text="{Binding FromDateString}"
                                                    TextColor="Gray" />
                                                <Span
                                                    FontFamily="LightFont"
                                                    Text=" to "
                                                    TextColor="Gray" />
                                                <Span
                                                    FontFamily="LightFont"
                                                    Text="{Binding ToDateString}"
                                                    TextColor="Gray" />
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                    <StackLayout
                                        Grid.Row="0"
                                        Grid.Column="2"
                                        HorizontalOptions="Fill"
                                        VerticalOptions="Center">
                                        <Frame
                                            Padding="0"
                                            BackgroundColor="{Binding StatusButtonBackgroundColor}"
                                            BorderColor="{Binding StatusButtonBorderColor}"
                                            CornerRadius="13"
                                            HeightRequest="25"
                                            HorizontalOptions="Center"
                                            VerticalOptions="Center"
                                            WidthRequest="95">
                                            <Label
                                                FontFamily="LightFont"
                                                FontSize="10"
                                                HorizontalOptions="Center"
                                                Text="{Binding LeaveStatus}"
                                                TextColor="{Binding StatusButtonTextColor}"
                                                TextTransform="Uppercase"
                                                VerticalOptions="Center" />
                                        </Frame>
                                    </StackLayout>
                                </Grid>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </RefreshView>
            <Frame
                Grid.Row="2"
                Margin="10,0,10,30"
                Padding="0"
                BackgroundColor="{StaticResource NewThemeTextColor}"
                CornerRadius="30"
                HasShadow="False"
                HeightRequest="60"
                HorizontalOptions="End"
                VerticalOptions="End"
                WidthRequest="60">
                <Label
                    FontFamily="LightIcons"
                    FontSize="30"
                    HorizontalOptions="Center"
                    Text="+"
                    TextColor="White"
                    VerticalOptions="Center" />
                <Frame.GestureRecognizers>
                    <TapGestureRecognizer Tapped="NewLeave_Tapped" />
                </Frame.GestureRecognizers>
            </Frame>
        </Grid>
    </ContentPage.Content>
</ContentPage> 