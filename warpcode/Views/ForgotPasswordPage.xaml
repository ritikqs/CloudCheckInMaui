<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="CCIMIGRATION.Views.ForgotPasswordPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converter="clr-namespace:CCIMIGRATION.Converter"
    xmlns:customControl="clr-namespace:CCIMIGRATION.CustomControls"
    xmlns:header="clr-namespace:CCIMIGRATION.CustomControls.CustomHeader"
    xmlns:icons="clr-namespace:CCIMIGRATION.Fonts"
    xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:translater="clr-namespace:CCIMIGRATION.MultilanguageHelper"
    ios:Page.UseSafeArea="true"
    BackgroundColor="{StaticResource White}"
    NavigationPage.HasNavigationBar="False">
    <ContentPage.Resources>
        <ResourceDictionary>
            <converter:InValidFrameColorConverter x:Key="ValidationErrorConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <Grid Padding="20,10" RowDefinitions="50,40,*">
            <Label
                Grid.Row="0"
                HorizontalOptions="Start"
                TextColor="Red"
                VerticalOptions="Center">
                <Label.FormattedText>
                    <FormattedString>
                        <Span FontFamily="SolidIcons" Text="{x:Static icons:Icons.ChevronLeft}" />
                        <Span
                            FontAttributes="Bold"
                            FontFamily="MediumFont"
                            FontSize="15"
                            Text="  Back" />
                    </FormattedString>
                </Label.FormattedText>
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding BackCommand}" />
                </Label.GestureRecognizers>
            </Label>
            <Label
                Grid.Row="1"
                Margin="10,0,10,0"
                FontFamily="MediumFont"
                FontSize="16"
                Text="{translater:Translate ForgotPassword}"
                TextColor="Black" />
            <StackLayout Grid.Row="2" Margin="10,0">
                <Grid RowDefinitions="50,Auto" RowSpacing="0">
                    <Button
                        Grid.Row="0"
                        Background="#e6e6e6"
                        BorderColor="Transparent"
                        CornerRadius="8"
                        HeightRequest="60"
                        HorizontalOptions="Fill"
                        IsEnabled="False"
                        VerticalOptions="Center" />
                    <customControl:CustomEntry
                        x:Name="email"
                        Grid.Row="0"
                        Margin="10,0"
                        FontFamily="LightFont"
                        Placeholder="{translater:Translate EmailPlaceHolder}"
                        PlaceholderColor="{StaticResource Gray}"
                        Text="{Binding Email, Mode=TwoWay}"
                        TextColor="Black" />
                    <Label
                        Grid.Row="1"
                        FontSize="12"
                        IsVisible="{Binding IsEmailNotValid}"
                        Text="{translater:Translate EmailValidation}"
                        TextColor="Red" />
                </Grid>
                <Frame
                    Margin="0,30,0,0"
                    HorizontalOptions="Center"
                    Style="{DynamicResource GradientFrame}"
                    VerticalOptions="Center">
                    <Label
                        FontFamily="Icons"
                        FontSize="30"
                        HorizontalOptions="Center"
                        Text="{x:Static icons:Icons.ArrowRight}"
                        TextColor="White"
                        VerticalOptions="Center" />
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding SubmitButtonCommand}" />
                    </Frame.GestureRecognizers>
                </Frame>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>